# objectified-service

This is the services layer of Objectified.

## Building

To build the project, run the following command:

```shekk
./build.sh
```

That's it!

To run the server, run:

```shell
yarn start
```

Open a browser and navigate to `http://localhost:3001/v1/api`.
