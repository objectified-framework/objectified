services:
  objectified-docs:
    container_name: objectified-docs
    expose:
      - ${PORT}
    ports:
      - "${PORT}:${PORT}"
    working_dir: /objectified-docs
    entrypoint: ["yarn", "serve"]
    command: []
    environment:
      - PORT=${PORT}
    build:
      context: .
      dockerfile: Dockerfile
    image: objectified-docs:${TAG_DOCS:-latest}
